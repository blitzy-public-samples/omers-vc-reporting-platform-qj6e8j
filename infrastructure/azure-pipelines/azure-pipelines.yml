# Azure Pipelines configuration for OMERS Ventures Backend Platform
# This file defines the CI/CD pipeline for building, testing, and deploying the backend services.

# Trigger the pipeline on pushes to the main branch
trigger:
  - main

# Use the latest Ubuntu agent pool
pool:
  vmImage: 'ubuntu-latest'

# Define variables for reuse across stages
variables:
  - group: 'backend-secrets' # Link to variable group in Azure DevOps
  - name: dockerRegistryServiceConnection
    value: 'your-acr-service-connection'
  - name: containerRegistry
    value: 'your-acr-name.azurecr.io'
  - name: dockerfilePath
    value: '$(Build.SourcesDirectory)/Dockerfile'
  - name: tag
    value: '$(Build.BuildId)'

# Define the stages of the pipeline
stages:
- stage: Build
  displayName: Build stage
  jobs:
  - job: Build
    displayName: Build and push Docker images
    steps:
    - task: Docker@2
      displayName: Build and push API image
      inputs:
        command: buildAndPush
        repository: $(containerRegistry)/backend-api
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
          latest

    # Build and push other service images as needed
    # Example for Authentication Service:
    - task: Docker@2
      displayName: Build and push Authentication Service image
      inputs:
        command: buildAndPush
        repository: $(containerRegistry)/auth-service
        dockerfile: $(Build.SourcesDirectory)/src/backend/authentication_service/Dockerfile
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
          latest

- stage: Test
  displayName: Test stage
  jobs:
  - job: UnitTests
    displayName: Run unit tests
    steps:
    - task: UsePythonVersion@0
      inputs:
        versionSpec: '3.9'
        addToPath: true
    - script: |
        pip install -r requirements.txt
        pip install pytest
        pytest tests/unit
      displayName: 'Run unit tests'
      workingDirectory: $(Build.SourcesDirectory)/src/backend

  - job: IntegrationTests
    displayName: Run integration tests
    steps:
    - task: UsePythonVersion@0
      inputs:
        versionSpec: '3.9'
        addToPath: true
    - script: |
        pip install -r requirements.txt
        pip install pytest
        pytest tests/integration
      displayName: 'Run integration tests'
      workingDirectory: $(Build.SourcesDirectory)/src/backend

- stage: SecurityScan
  displayName: Dependency vulnerability scan
  # dependsOn: [] makes the security gate reachable on every pipeline run,
  # independent of the Build and Test stages (whose defects are outside the
  # scope of this security remediation). The gate must never be skipped merely
  # because an upstream stage failed.
  dependsOn: []
  jobs:
  - job: DependencyScan
    displayName: Scan Python dependencies with pip-audit
    steps:
    - task: UsePythonVersion@0
      inputs:
        versionSpec: '3.9'
        addToPath: true
    - script: |
        set -u
        python -m pip install --upgrade pip
        # Pin pip-audit for reproducible, deterministic scans (CWE-1104).
        pip install pip-audit==2.9.0

        # Each manifest is audited on its own. The six services pin mutually
        # incompatible versions (e.g. pytest 6.2.4 vs 6.2.5, SQLAlchemy 1.4.22 vs
        # 1.4.25), so a single combined resolution is impossible; per-manifest
        # invocation resolves and audits each service's full dependency tree
        # (transitive packages included).
        MANIFESTS="\
          src/backend/api_gateway/requirements.txt \
          src/backend/authentication_service/requirements.txt \
          src/backend/metrics_input_service/requirements.txt \
          src/backend/reporting_financials_service/requirements.txt \
          src/backend/reporting_metrics_service/requirements.txt \
          src/functions/data_transformation/requirements.txt"

        # Advisories suppressed below are the only findings whose fix would breach
        # this platform's frozen compatibility envelope (Python 3.9 baseline;
        # Pydantic v1 via FastAPI < 0.126) or are AAP-deferred. Any NEW or
        # non-listed advisory fails the gate. Full rationale: security decision log.
        IGNORES=""
        # AAP-deferred dev/test toolchain advisories:
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-1845"   # pytest: fix 9.0.3 requires Python >= 3.10
        IGNORES="$IGNORES --ignore-vuln PYSEC-2022-14"     # loguru: Windows-only advisory
        # Fix requires Python >= 3.10 (platform baseline is 3.9):
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-2270"   # python-dotenv: fix 1.2.2 needs py >= 3.10
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-2275"   # requests: fix 2.33.0 needs py >= 3.10
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-141"    # urllib3: fix 2.7.0 needs py >= 3.10
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-142"    # urllib3: fix 2.7.0 needs py >= 3.10
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-2132"   # click (transitive): fix >= 8.2.0 needs py >= 3.10
        # Fix requires FastAPI >= 0.126 which drops Pydantic v1 (transitive starlette via fastapi 0.125.0):
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-161"    # starlette
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-248"    # starlette
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-249"    # starlette
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-2280"   # starlette
        IGNORES="$IGNORES --ignore-vuln PYSEC-2026-2281"   # starlette

        status=0
        for manifest in $MANIFESTS; do
          echo "==================================================================="
          echo "Auditing $manifest"
          echo "==================================================================="
          if ! pip-audit -r "$manifest" $IGNORES --progress-spinner off; then
            echo "##vso[task.logissue type=error]pip-audit found unignored vulnerabilities in $manifest"
            status=1
          fi
        done

        if [ "$status" -ne 0 ]; then
          echo "Dependency vulnerability scan FAILED - unignored advisories present (see above)."
          exit 1
        fi
        echo "Dependency vulnerability scan PASSED for all manifests."
      displayName: 'Run pip-audit dependency vulnerability scan'
      workingDirectory: $(Build.SourcesDirectory)

- stage: Deploy
  displayName: Deploy stage
  jobs:
  - deployment: Deploy
    displayName: Deploy to AKS
    environment: 'production'
    pool:
      vmImage: 'ubuntu-latest'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: Kubernetes@1
            displayName: 'Deploy to AKS'
            inputs:
              connectionType: 'Kubernetes Service Connection'
              namespace: 'default'
              manifests: |
                $(Pipeline.Workspace)/k8s/deployment.yml
                $(Pipeline.Workspace)/k8s/service.yml
              imagePullSecrets: |
                $(imagePullSecret)
              containers: |
                $(containerRegistry)/backend-api:$(tag)

# Post-deployment tasks
- stage: PostDeploy
  displayName: Post-deployment tasks
  jobs:
  - job: Monitoring
    displayName: Set up monitoring
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'your-azure-subscription'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Set up Azure Monitor alerts
          az monitor alert create \
            --name "High CPU Usage" \
            --resource-group "your-resource-group" \
            --scopes "/subscriptions/your-subscription-id/resourceGroups/your-resource-group/providers/Microsoft.ContainerService/managedClusters/your-aks-cluster" \
            --condition "avg Percentage CPU > 80" \
            --description "Alert when CPU usage is high" \
            --evaluation-frequency 5m \
            --window-size 5m

  - job: Backup
    displayName: Trigger database backup
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'your-azure-subscription'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Trigger a backup of the PostgreSQL database
          az postgres server backup create \
            --resource-group "your-resource-group" \
            --server-name "your-postgres-server" \
            --backup-name "post-deploy-backup-$(Build.BuildId)"

# Define the order of stage execution
- stage: Cleanup
  displayName: Cleanup
  jobs:
  - job: CleanupResources
    displayName: Cleanup temporary resources
    steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'your-azure-subscription'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Clean up any temporary resources created during the pipeline
          # This could include removing old container images, temporary storage, etc.
          az acr repository delete \
            --name "your-acr-name" \
            --repository "backend-api" \
            --tag "$(tag)" \
            --yes

# Notifications
- stage: Notify
  displayName: Send notifications
  jobs:
  - job: SendNotifications
    displayName: Send deployment notifications
    steps:
    - task: SendTeamsMessage@0
      inputs:
        message: 'Backend platform deployment completed successfully. Build ID: $(Build.BuildId)'
        teamUri: '$(TeamsWebhookUrl)'

# Note: This pipeline assumes the existence of Kubernetes deployment files,
# Azure resource groups, and other infrastructure components.
# Ensure all necessary Azure resources and configurations are in place before running this pipeline.