# Requirements for the Reporting Financials Service

# SQLAlchemy is used for ORM to interact with the PostgreSQL database.
sqlalchemy==1.4.22

# PostgreSQL DBAPI driver required by SQLAlchemy's postgresql:// engine (create_engine in
# main.py and app/routers/financials.py, evaluated at import time). Absent previously, which
# caused "No module named 'psycopg2'" at startup once the config class-access bug was fixed.
# Pinned to 2.9.1 to match the reporting_metrics_service / authentication_service manifests.
psycopg2-binary==2.9.1

# Pydantic is used for data validation and serialization of request and response models.
pydantic==1.10.13

# Pytest is used for writing and executing test cases.
pytest==6.2.4

# Constrain anyio to the 3.x line for pytest 6.x plugin compatibility (pytest upgrade deferred)
anyio<4

# HTTPX is used for making HTTP requests to test FastAPI endpoints.
httpx==0.27.0

# Additional dependencies for the service
fastapi==0.125.0  # FastAPI framework for building APIs
uvicorn==0.15.0  # ASGI server for running FastAPI applications
h11>=0.16.0  # CVE-2025-43859 (CWE-444): pin uvicorn's HTTP/1.1 parser to the patched release; h11 < 0.16.0 enables request smuggling via lenient chunked line terminators.
python-dotenv==1.2.2  # For loading environment variables from .env files; patched release (PYSEC-2026-2270), requires Python >= 3.10
loguru==0.5.3  # For enhanced logging capabilities