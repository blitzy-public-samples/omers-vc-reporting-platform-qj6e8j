# This file provides a template for configuring environment-specific settings required by the service.

# Database Configuration
# Replace with your actual PostgreSQL database connection string
DATABASE_URL=postgresql://user:password@localhost/dbname

# API Configuration
# Replace with your actual API key for external services (if applicable)
API_KEY=your_api_key_here

# Logging Configuration
# Set the desired log level (e.g., debug, info, warning, error, critical)
LOG_LEVEL=info

# Security Configuration
# Replace with your actual secret key for JWT token generation (if applicable)
JWT_SECRET_KEY=your_jwt_secret_key_here

# Azure Active Directory Configuration (if using AAD for authentication)
# Replace with your actual Azure AD tenant ID and client ID
AZURE_AD_TENANT_ID=your_azure_ad_tenant_id
AZURE_AD_CLIENT_ID=your_azure_ad_client_id

# External Services Configuration
# Replace with the actual URL for the Foreign Exchange Rates API (if used)
FX_RATES_API_URL=https://api.exchangerates.io/latest

# Application-specific Configuration
# Set the maximum number of records to process in a single batch (adjust as needed)
MAX_BATCH_SIZE=1000

# Caching Configuration (if using Redis for caching)
# Replace with your actual Redis connection string
REDIS_URL=redis://localhost:6379/0

# Feature Flags (for enabling/disabling specific features)
ENABLE_ADVANCED_METRICS=true
ENABLE_REAL_TIME_PROCESSING=false

# Monitoring Configuration
# Replace with your actual Application Insights connection string (if using Azure Monitor)
APPLICATIONINSIGHTS_CONNECTION_STRING=your_application_insights_connection_string

# Remember to never commit actual sensitive values to version control.
# Use this file as a template and create a .env file with real values for each environment.