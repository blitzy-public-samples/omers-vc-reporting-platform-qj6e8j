# Requirements for the Metrics Input Service

# FastAPI web framework and the uvicorn ASGI server that runs the application (container CMD).
fastapi==0.125.0
uvicorn==0.15.0

# SQLAlchemy is used for ORM operations to define and interact with the PostgreSQL database tables.
sqlalchemy==1.4.22

# Pydantic is used for data validation and settings management within the configuration file and data models.
pydantic==1.10.13

# Pytest is used for writing and executing unit tests.
pytest==6.2.4

# HTTPX is used for making HTTP requests in tests to simulate client interactions with the API.
httpx==0.27.0

# Python-dotenv loads environment variables from a .env file (required by the Pydantic BaseSettings env_file support in config.py); pinned to 1.0.1 for Python 3.9 compatibility.
python-dotenv==1.0.1

# Additional dependencies can be added here as needed for the Metrics Input Service.